# Notebooks (optional)

The pipeline itself lives entirely in `src/*.py`, not in notebook cells, so that it is directly runnable via `make` and testable via `pytest`. This folder is left for optional exploratory work only (e.g. extra EDA plots for the report) -- nothing here is required to reproduce the headline results. See the root `README.md`.
